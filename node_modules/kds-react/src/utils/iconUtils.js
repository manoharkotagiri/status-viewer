import React from 'react'
import { isComponentWithName, getDataName } from './componentUtils'

const hasIconName = maybeIcon =>
  isComponentWithName(maybeIcon, /^Icon/) || getDataName(maybeIcon) === 'Icon'

export const isKDSIcon = maybeIcon =>
  React.isValidElement(maybeIcon) && hasIconName(maybeIcon)

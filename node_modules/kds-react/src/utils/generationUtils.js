/**
 * This file should contain any utilities that randomly generate values.
 * This way you can mock this file specifically in tests
 */

let last = null
export const generateUniqueString = () => {
  const now = Date.now()
  last = now > last ? now : last + 1
  return last.toString(36)
}

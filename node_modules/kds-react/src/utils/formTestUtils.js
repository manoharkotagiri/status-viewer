import React from 'react'
import { render } from 'react-testing-library'
import Form from '../components/Form/Form'

export const renderWithForm = formField => {
  const container = render(<Form>{formField}</Form>)
  return {
    ...container,
    getForm: () => container.baseElement.querySelector('form')
  }
}

import React from 'react'
import { KdsIconErrorFilled } from 'kds-stencil-react-lite'
import FilledIcon from '../common/FilledIcon'

/** See [`FilledIcon`](./#/Components/FilledIcon) documentation for further details. */

const FilledIconError = props => {
  return <FilledIcon as={KdsIconErrorFilled} {...props} />
}

export default FilledIconError

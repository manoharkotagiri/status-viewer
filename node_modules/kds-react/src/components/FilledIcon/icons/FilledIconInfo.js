import React from 'react'
import { KdsIconInfoFilled } from 'kds-stencil-react-lite'
import FilledIcon from '../common/FilledIcon'

/** See [`FilledIcon`](./#/Components/FilledIcon) documentation for further details. */

const FilledIconInfo = props => {
  return <FilledIcon as={KdsIconInfoFilled} {...props} />
}

export default FilledIconInfo

import React from 'react'
import { KdsIconWarningFilled } from 'kds-stencil-react-lite'
import FilledIcon from '../common/FilledIcon'

/** See [`FilledIcon`](./#/Components/FilledIcon) documentation for further details. */

const FilledIconWarning = props => {
  return <FilledIcon as={KdsIconWarningFilled} {...props} />
}

export default FilledIconWarning

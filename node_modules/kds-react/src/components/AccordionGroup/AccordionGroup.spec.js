import React from 'react'
import AccordionGroup from './AccordionGroup'
import { render } from 'react-testing-library'
import SectionAccordion from '../Accordion/SectionAccordion/SectionAccordion'

describe('AccordionGroup component', () => {
  it('renders expected output', () => {
    const { container } = render(
      <AccordionGroup className="mySpecialAccordionGroup">
        <SectionAccordion className="childClassOne" id="acc-one" />
        <SectionAccordion className="childClassTwo" id="acc-two" />
        <SectionAccordion className="childClassThree" id="acc-three" />
      </AccordionGroup>
    )

    expect(container.firstChild).toMatchSnapshot()
  })
})

import React, { Component } from 'react'
import { Consumer } from './_ToastContext'

/** See [`Toast`](./toast) documentation for further details. */

class ToastConsumer extends Component {
  render () {
    return <Consumer>{context => this.props.children(context)}</Consumer>
  }
}

export default ToastConsumer

import { s as setMode } from './index-7f5ea1ce.js';

const themes = ["ecommerce", "admin"];
const mode = (typeof document !== "undefined" &&
  document.documentElement.getAttribute("kds-theme")) ||
  "admin";
setMode(() => (themes.includes(mode) ? mode : "admin"));

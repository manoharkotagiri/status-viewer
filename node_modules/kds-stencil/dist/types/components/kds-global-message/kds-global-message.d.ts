import { EventEmitter } from "../../stencil-public-runtime";
interface KdsGlobalMessageHostElement extends HTMLElement {
  nativeElement: HTMLElement;
}
/**
 * Use a kds-global-message to communicate application-wide information with users through the User Interface (UI).
 */
export declare class KdsGlobalMessage {
  hostEl: KdsGlobalMessageHostElement;
  /** Determines the background color and which icon to display. */
  kind: "info" | "error" | "warning";
  /** Heading text. */
  heading: string;
  /** The message text.
   * Use this prop **or** provide your message as a child to the component. See [Examples](#Examples) */
  message?: string;
  /** Determines if closable. */
  closable: boolean;
  /** An event fired when a user closes the message. */
  messageClosed: EventEmitter;
  show: boolean;
  ariaMessage: string;
  private kindToIconMap;
  private kindToRoleMap;
  onCloseButtonClick: () => void;
  componentWillLoad(): void;
  render(): any;
}
export {};

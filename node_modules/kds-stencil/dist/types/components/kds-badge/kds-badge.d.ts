/**
 * A kds-badge displays a numeric value that represents a quantity of items or elements.
 */
export declare class KdsBadge {
  hostEl: HTMLElement;
  /** The numeric value to display. */
  count?: number;
  /** Determines the background color. */
  color?: "brand" | "inverse" | "positive" | "negative" | "neutral" | "special" | "callout" | string;
  private isHydrated;
  private outputElement;
  componentDidLoad(): void;
  render(): any;
}

import { EventEmitter } from "../../stencil-public-runtime";
interface KdsTagHostElement extends HTMLElement {
  nativeElement: HTMLElement;
}
/**
 * Tags are compact elements that help organize and categorize content.
 */
export declare class KdsTag {
  hostEl: KdsTagHostElement;
  /**
   * ```Message { "props": { "kind": "warning", "className": "mb-16" } }
   * **Deprecation Warning:** `interactive` kind is in the process of being deprecated. Please use `action` instead.
   * ```
   * Sets the color scheme.
   * */
  kind: "special" | "default" | "positive" | "callout" | "negative" | "action" | "neutral" | "interactive";
  /** Defines icon to show left of the text. */
  icon: string;
  /**
   * ```Message { "props": { "kind": "warning", "className": "mb-16" } }
   * **Deprecation Warning:** This property is in the process of being deprecated. Do not use in production.
   * ```
   * Sets text.
   * */
  text?: string;
  /** Applies the compact size. */
  compact?: boolean;
  ariaMessage: string;
  /** An event fired when a user removes a tag. */
  tagRemoved: EventEmitter;
  onRemoveTagClick: () => void;
  componentWillLoad(): void;
  render(): any;
}
export {};

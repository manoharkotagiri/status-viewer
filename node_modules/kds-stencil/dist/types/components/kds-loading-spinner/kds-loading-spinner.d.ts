/**
 * Loading indicators communicate the status of an activity happening in the background. They're used to provide the user feedback as they navigate and perform complex actions.
 */
export declare class KdsLoadingSpinner {
  /** Sets a light color scheme for dark backgrounds. */
  inverse: boolean;
  /** Indicates what the loading spinner is for. Used for accessibility purposes. */
  label: string;
  /** Controls the dimensions and font-size. */
  size: "s" | "m" | "l" | "xs";
  render(): any;
}

import { EventEmitter } from "../../stencil-public-runtime";
/**
 * Use tabs to organize related content into multiple views.
 * Tabs are useful for switching between a small number of views.
 * For example, switching between products, coupons and recipes in a search results page.
 */
export declare class KdsTabs {
  uniqueId: string;
  hostEl: HTMLElement;
  /** Sets class(es) on tab container. */
  tabsClass: string;
  /** Sets class(es) on content container. */
  contentClass: string;
  /** Defines default selected tab. */
  defaultSelected: number;
  /** Emits when tabs are changed. Event `detail` contains index of newly selected tab. */
  tabChange: EventEmitter<number>;
  private tabEls;
  tabButton: Array<HTMLButtonElement>;
  selectedIndex: number;
  private childObserver;
  update(): Promise<void>;
  componentWillLoad(): void;
  handleKeyDown(event: any): void;
  /** Sets and focuses the selected tab to the given index.
   * @index The index of the tab to be selected.
   */
  selectTab(index: number): Promise<void>;
  render(): any;
}

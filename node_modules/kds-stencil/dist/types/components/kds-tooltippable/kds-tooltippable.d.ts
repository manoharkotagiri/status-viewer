/// <reference types="node" />
/**
 * A component used to attach a tooltip to an element.
 * Tooltips allow users to get contextual help or information about specific components when hovering or focusing them.
 */
export declare class KdsTooltippable {
  hostEl: HTMLElement;
  /** Sets the side a tooltip will appear relative to it's attached element. */
  side: "top" | "bottom" | "left" | "right";
  /** Sets how the tooltip will align with it's attached element. */
  align: "center" | "left" | "right";
  /** Sets the text that will render inside the tooltip. */
  tooltipText: string;
  /** Determines if the tooltip acts as a label or description of the tooltipped element and sets proper aria attributes. */
  tooltipType: "label" | "description";
  currentSide: "top" | "bottom" | "left" | "right";
  currentAlign: "center" | "left" | "right";
  hideTooltip: boolean;
  updateSide(newSide: any): void;
  updateAlign(newAlign: any): void;
  intersectionCount: number;
  tooltipContent: HTMLDivElement;
  tooltip: HTMLDivElement;
  tooltipId: string;
  handleIntersect: (entries: any) => void;
  observer: IntersectionObserver;
  intersectionTimeout: NodeJS.Timeout;
  shouldAlignCenter(): boolean;
  registerObserver(): void;
  disconnectObserver(): void;
  render(): any;
}

interface KdsTextAreaHostElement extends HTMLElement {
  nativeElement: HTMLElement;
}
/**
 * Allows the user to enter large amounts of text. This can be used for collecting feedback or long form responses.
 */
export declare class KdsTextArea {
  hostEl: KdsTextAreaHostElement;
  /** Disables if present. */
  disabled?: boolean;
  /** Associates the field to a form element. */
  form?: string;
  /** Indicates the validity of the value. */
  ariaInvalid?: string;
  /** Defines the maximum number of characters allowed. */
  maxlength?: number;
  /** Defines the minimum number of characters allowed. */
  minlength?: number;
  /** Sets the name. */
  name?: string;
  /** Displays helpful information for the user. */
  placeholder?: string;
  /** Sets to read-only if present. */
  readonly?: boolean;
  /** Requires value before submission. */
  required?: boolean;
  /** Contains the current value. */
  value?: string;
  /** Contains the current number of characters entered. */
  count: number;
  renderMaxLengthMessage: boolean;
  private nativeElement;
  private onInput;
  componentWillLoad(): void;
  componentDidLoad(): void;
  removeInlineStyle(): void;
  /**
   * Returns the native `<textarea>` element.
   */
  getNativeElement(): Promise<HTMLElement>;
  checkLength(): void;
  render(): any;
}
export {};

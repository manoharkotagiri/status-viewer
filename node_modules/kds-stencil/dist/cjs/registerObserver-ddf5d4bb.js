'use strict';

const attributeUtils = require('./attributeUtils-a46de586.js');

const registerSlotObserver = (component, callback) => {
  return new MutationObserver(callback.bind(component));
};
const registerAttributeObserver = component => {
  const { hostEl } = component;
  return new MutationObserver(mutations => {
    // Create a new object from current native attributes
    const newAttrs = Object.assign({}, component.innerAttrs);
    mutations.forEach(({ attributeName }) => {
      const unprefixedName = attributeName.replace(new RegExp(`^${attributeUtils.innerPrefix}`), "");
      const value = hostEl.getAttribute(attributeName);
      if (hostEl.hasAttribute(attributeName) &&
        attributeUtils.innerPattern.test(attributeName)) {
        // Properly assign the attribute to newAttrs object.
        attributeUtils.setNativeAttribute(unprefixedName, value, newAttrs);
      }
      else if (attributeUtils.defaultHostAttributes[attributeName] &&
        value !== attributeUtils.defaultHostAttributes[attributeName]) {
        console.warn(`Attempted to set "${attributeName}". Use "inner-${attributeName}" instead`);
        hostEl.setAttribute(attributeName, attributeUtils.defaultHostAttributes[attributeName]);
      }
      else if (newAttrs[unprefixedName]) {
        delete newAttrs[unprefixedName];
      }
    });
    /** Check if attribute objects are equal before assigning innerAttrs state object,
        trigging a re-render */
    if (!isEquivalent(component.innerAttrs, newAttrs)) {
      component.innerAttrs = Object.assign({}, newAttrs);
    }
  });
};
function isEquivalent(a, b) {
  // Create arrays of property names
  var aProps = Object.getOwnPropertyNames(a);
  var bProps = Object.getOwnPropertyNames(b);
  // If number of properties is different,
  // objects are not equivalent
  if (aProps.length != bProps.length) {
    return false;
  }
  for (var i = 0; i < aProps.length; i++) {
    var propName = aProps[i];
    // If values of same property are not equal,
    // objects are not equivalent
    if (a[propName] !== b[propName]) {
      return false;
    }
  }
  // If we made it this far, objects
  // are considered equivalent
  return true;
}

exports.registerAttributeObserver = registerAttributeObserver;
exports.registerSlotObserver = registerSlotObserver;
